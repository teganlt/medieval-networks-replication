# Python 3.12. Versions used for the paper run (looser versions likely work,
# but digit-exact replication was verified with these).
pandas==3.0.1
numpy==2.4.3
matplotlib==3.10.8
python-igraph==1.0.0
scipy==1.17.1
leidenalg==0.11.0
# Only needed to RE-RUN the generative-AI stages (not run by default):
# anthropic
